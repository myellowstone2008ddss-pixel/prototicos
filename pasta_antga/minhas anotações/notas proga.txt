notas proga
if keyboard_check(+tecla)=a se mover
y-=para cima
y+=para baixo
x-=para esquerda
x+=para direita
tamanho tela = window_set_size(1920,1080)

nota
#region movimento / colisão
direita = keyboard_check(ord("D"));
esquerda = keyboard_check(ord("A"));
cima = keyboard_check(ord("W"));
baixo = keyboard_check(ord("S"));
xsp = (direita - esquerda) * sp;
if place_meeting(x +xsp , y ,parede_obj)
{while !place_meeting(x + sign(xsp),y,parede_obj)
{x += sign(xsp)	} xsp = 0 }
x += xsp;
ysp = (baixo - cima) * sp;
if place_meeting(x, y + ysp,parede_obj)
{while !place_meeting(x,y + sign(ysp),parede_obj)
{ y += sign(ysp)	} ysp = 0 }

y += ysp
#endregion
#region direção
if keyboard_check(ord("D")){ dir = 0}
if keyboard_check(ord("A")){ dir = 2}
if keyboard_check(ord("W")){ dir = 1}
if keyboard_check(ord("S")){ dir = 3}
if xsp == 0 and ysp ==0 {
switch dir {
	default: sprite_index = spr_personagem_parado_direita
	break;
	case 1: sprite_index = spr_personagem_parado_cima
	break;
	case 2: sprite_index = spr_personagem_parado_esquerda
	break;
	case 3: sprite_index = spr_personagem_parado_baixo
	break;}}
	else {
		switch dir {
	default: sprite_index = spr_personagem_correndo_direita
	break;
	case 1: sprite_index = spr_personagem_correndo_cima
	break;
	case 2: sprite_index = spr_personagem_correndo_esquerda
	break;
	case 3: sprite_index = spr_personagem_correndo_baixo
	break;}}
	#endregion